# secrets / local config — never commit
config.properties
local.properties
*.keystore
*.jks
*.pem
*.key
.env
.env.*

# build
.gradle/
build/
app/build/
*.apk
*.aab
*.ap_
captures/
.externalNativeBuild/
.cxx/

# IDE
.idea/
*.iml
.kotlin/
.DS_Store

# raw label mirrors and local data at the repo root — never committed (privacy law)
/data/
tools/watch/.last-watch-ip

# compiled Python: churns on every scheduled run, carries no information
__pycache__/
*.pyc
